<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
  <title>Yet another message board</title>
  <style>
    body {
      max-width: 600px;
      margin: auto;
      font-family: Arial, Helvetica, sans-serif;
      color: #333;
      background: #F5F5F5;
      padding: 1em;
      line-height: 1.6;
    }

    li {
      list-style: none;
    }

    ul {
      padding-left: 0;
    }

    .error {
      background: rgb(182, 70, 70);
      padding: .5em;
      color: white;
      margin-bottom: .2em;
    }
  </style>
</head>

<body>
  {{#each errors}}
  <div class="error">{{this}}</div>
  {{/each}}
  {{{body}}}
</body>

</html>